CHIP 2AddressDecoder4 {
    IN in, sel[2];
    OUT a, b, c, d;
    PARTS:
        DMux(in=in, sel=sel[1], a=outAorB, b=outCorD);
        DMux(in=outAorB, sel=sel[0], a=a, b=b);
        DMux(in=outCorD, sel=sel[0], a=c, b=d);
}
